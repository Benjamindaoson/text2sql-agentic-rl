# 课程来源映射

- 原始课程代码快照：`D:\01_project\找工作的项目\agentic RL\项目源码\spider`。
- 快照：原有 `sql_agent.py`、`train_sql_agent.py` 与 `spider_eval/` 位于 `legacy_reproduction/spider/`，未做改写。
- 不复制：Spider 训练数据；保留课程中的规范数据位置引用。
- 升级内容：只读 SQLite 沙箱、执行结果等价、轨迹状态机和稠密奖励，位于 `upgraded_implementation/`。
